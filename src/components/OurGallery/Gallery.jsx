import React from 'react'

const Gallery = () => {
    // Here you fetch the galleries from database. And then render them here. Each card will have a image, title, description and edit and delete buttons.
  return (
    <div>Gallery here for admin</div>
  )
}

export default Gallery